# Cargo.toml
[package]
name = "unified-dns"
version = "1.1.0"
edition = "2021"
description = "A high-performance iterative recursive DNS resolver supporting DNS, DNS over TLS, and DNS over HTTPS."
license = "MIT"
repository = "https://github.com/AZBrandCanada/Rust-Recursive-DNS-Resolver"
homepage = "https://github.com/AZBrandCanada/Rust-Recursive-DNS-Resolver"
readme = "README.md"
keywords = ["dns", "dnssec", "resolver", "dot", "doh"]
categories = ["network-programming"]
exclude = [
    "GeoLite2-City.mmdb", 
]

[dependencies]
axum = { version = "0.8", default-features = false, features = ["tokio", "http1", "http2", "query", "matched-path"] }
axum-server = { version = "0.7", features = ["tls-rustls"] }
tokio = { version = "1.43", features = ["rt-multi-thread", "net", "io-util", "time", "macros", "signal", "sync", "fs"] }
hickory-proto = { version = "0.25", features = ["dnssec-ring"] }
ring = "0.17"
sha2 = "0.10"
dashmap = { version = "6.1", features = ["serde"] }
maxminddb = "0.24"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
rand = "0.8"
thiserror = "2.0"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
zip = { version = "2.2", default-features = false, features = ["deflate"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["ring"] }
rustls-pemfile = "2.2"
rustls-pki-types = "1.10"
rcgen = "0.13"
ml-dsa = "0.1"
quinn = { version = "0.11", default-features = false, features = ["runtime-tokio", "rustls", "ring"] }
h3 = "0.0.8"
h3-quinn = "0.0.10"
bytes = "1.12.1"
http = "1.5.0"
rsa = { version = "0.9", features = ["sha2", "sha1"] }
sha1 = "0.10"
moka = { version = "0.12", features = ["sync"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
incremental = false
overflow-checks = true
